---
name: life
description: >
  Self-model and wisdom capture for John. USE WHEN the user wants to
  update telos (goals, vision, mindset, identity) or capture a quote.
  Daily/weekly rhythm lives in `/routine`. All sub-skills read/write
  `~/helm/02-ana/`.
---

# Life

Self-model maintenance + wisdom capture. The slow-moving parts of John's
Life OS — identity narrative and the quotes that shape it. Daily/weekly
rhythm is in `/routine`.

## Routing table

| Task | Sub-skill | File to Read |
|------|-----------|--------------|
| Capture wisdom (external quote OR personal aphorism) by theme | quote | `quote.md` |
| Life OS — goals, beliefs, models, identity update | telos | `telos.md` |

## How to use

1. Pick the sub-skill by artifact.
2. `Read` the file in this directory.
3. Follow that file's instructions.

## When two could fit

- **quote vs telos:** quote captures discrete wisdom units; telos updates the long-lived self-model narrative.

## Cross-references

- Daily/weekly rhythm → `/routine/journal`, `/routine/today-prep`, `/routine/tomorrow-prep`, `/routine/weekly-retro`
- Cross-session memory retrieval → `/recall/history`
